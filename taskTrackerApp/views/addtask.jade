extends layout

block content
    .container

        h1= title
        form#formAddTask(name="addtask",method="post",action="/addtask")
            input.input-block-level#inputTaskName(type="text", placeholder="Task Name", name="taskname")
            #[br]
            #[br]
            input#inputTaskDesc(type="text", placeholder="Task Description", name="taskdesc")
            #[br]
            #[br]
            button.btn.btn-primary#btnSubmit(type="submit") Submit